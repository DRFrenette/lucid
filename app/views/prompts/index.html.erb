<h1>Prompts</h1>
  <% if current_user.admin? %>
    <div><%= link_to "Create a New Category", new_category_path %></div>
  <% end %>
<div><%= link_to "Create New Prompt", [:new, :prompt] %></div>
<div class="prompts"><%= render @prompts.order("created_at DESC") %></div>
